---
const { src, alt, caption, isVA } = Astro.props;
---

<figure>
  <img
    src={src}
    alt={alt}
    class:list={["shadow-md mx-auto", isVA && "max-h-[768px]"]}
  />
  <figcaption class="italic text-center">{caption}</figcaption>
</figure>
